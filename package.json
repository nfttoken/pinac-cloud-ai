{
  "name": "pinac-cloud-ai",
  "version": "1.0.0",
  "description": "API server that provides streaming chat completions using Workers AI models.",
  "devDependencies": {
    "typescript": "5.8.3",
    "wrangler": "4.12.0"
  },
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {}
}
